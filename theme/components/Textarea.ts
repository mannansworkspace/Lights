import Input from "./Input";

const variants = {
  floating: Input.variants.floating.field,
};

const defaultProps = {
  variant: "floating",
};

const Textarea = {
  variants,
  defaultProps,
};

export default Textarea;
